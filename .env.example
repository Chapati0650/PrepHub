# Google OAuth Configuration
# Get these values from Google Cloud Console > APIs & Services > Credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API
# 4. Go to APIs & Services > Credentials
# 5. Create OAuth 2.0 Client ID
# 6. Add your domain to authorized origins:
#    - For development: http://localhost:5173
#    - For WebContainer: Add your current WebContainer URL
#    - For production: your deployed domain
# 7. Copy the Client ID and paste it above
# 8. Rename this file to .env
#
# Note: For WebContainer environments, you may need to:
# - Use popup mode for OAuth (ux_mode="popup")
# - Disable one-tap sign-in (useOneTap={false})
# - Add the exact WebContainer URL to Google Console authorized origins

# Supabase Configuration
# Get these values from your Supabase project dashboard > Settings > API
VITE_SUPABASE_URL=https://ehlklfopwpmgkthqmqgd.supabase.co